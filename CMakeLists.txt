cmake_minimum_required( VERSION 2.8 )
project(HackThePlanet)
enable_language( CXX )
include_directories( "include" )
FIND_LIBRARY( Rendersystem_GL REQUIRED )
SET( HackThePlanet_SRCS src/hacktheplanet.cpp )
add_executable( HackThePlanet ${HackThePlanet_SRCS} )
target_link_libraries( HackThePlanet rt boost_random boost_filesystem boost_iostreams boost_date_time boost_chrono GL glut )
